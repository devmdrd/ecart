<style>
  @keyframes fade-in-down {
    0% {
      opacity: 0;
      transform: translateY(-20px);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }
  @keyframes pop {
    0% {
      transform: scale(0.95);
      opacity: 0;
    }
    100% {
      transform: scale(1);
      opacity: 1;
    }
  }
  .animate-fade-in-down {
    animation: fade-in-down 0.5s ease-out both;
  }
  .animate-pop {
    animation: pop 0.4s ease-out both;
  }
  .font-poppins {
    font-family: "Poppins", sans-serif;
  }
</style>

<div class="min-h-screen flex items-center justify-center font-poppins px-4">
  <div class="bg-white rounded-3xl shadow-2xl p-8 sm:p-10 w-full max-w-md text-center animate-fade-in-down">
    <div class="bg-green-500 text-white rounded-full w-16 h-16 flex items-center justify-center mx-auto mb-6 animate-pop">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
      </svg>
    </div>
    <h1 class="text-2xl sm:text-3xl font-extrabold text-gray-800 mb-2">Order Confirmed</h1>
    <p class="text-gray-500 mb-8 text-sm sm:text-base">
      Thank you for your purchase. Your order was placed successfully.
    </p>
    <div class="flex flex-col gap-3">
      <a href="/" class="bg-green-500 hover:bg-green-600 text-white font-semibold py-3 rounded-xl shadow-md transition-all duration-300 text-sm sm:text-base">
        Back to Home
      </a>
      <a href="/users/accounts/#orders" class="bg-gray-100 hover:bg-gray-200 text-gray-700 font-semibold py-3 rounded-xl transition-all duration-300 text-sm sm:text-base">
        View My Orders
      </a>
    </div>
  </div>
</div>
