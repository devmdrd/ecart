<div class="w-full px-1 text-center cursor-pointer">
    <div class="mb-1 relative">
      <img
        src="/<%- category.image %>"
        onerror="this.onerror=null; this.src='https://via.placeholder.com/100x50?text=Category';"
        class="h-12 w-auto mx-auto object-contain"
        alt="<%- category.name %>"
      />
      <!-- Hover overlay -->
      <div class="absolute inset-0 flex items-center justify-center bg-black bg-opacity-60 text-white text-xs font-semibold opacity-0 group-hover:opacity-100 transition-opacity">
        <%= category.name %>
      </div>
    </div>
    <h3 class="text-xs font-medium text-gray-800 hover:text-blue-600 transition-colors">
      <%= category.name %>
    </h3>
  </a>
</div>
